# OS
.DS_Store
Thumbs.db

# Editores
.vscode/
.idea/
*.swp

# Ambientes / dependências
.venv/
venv/
node_modules/
__pycache__/
*.pyc

# Segredos
.env
.env.*
!.env.example
credentials.json
*credentials*.json
service-account*.json
.credentials/
*.pem
*.key

# Temporários
tmp/
*.tmp
~$*

# Cache de coletas das skills (regenerável)
dados/cache/

# Masters de vídeo do data room. Pesados demais para o git (455 MB em clipes de 10s
# exportados a 30-51 Mbps). O repositório versiona proxies H.264 leves nos mesmos
# caminhos; o master fica aqui, na máquina de quem baixou, e no data room da OLX,
# que é a fonte canônica. SHA-256 de cada um em assets/originais/README.md.
assets/originais/_masters/

# Vercel
.vercel/

# eKyte: token do MCP, um por pessoa da squad. Nunca versionado: token commitado
# não sai do histórico, nem com o repo privado. Ver eKyte/CLAUDE.md#segurança.
eKyte/tokens.json
eKyte/.mcp.json
eKyte/.operador
